export enum UserRole {
  VIEWER = "viewer",
  ANALYST = "analyst",
  ADMIN = "admin"
}

export enum UserStatus {
  ACTIVE = "active",
  INACTIVE = "inactive"
}

export enum FinancialRecordType {
  INCOME = "income",
  EXPENSE = "expense"
}
